1. Get the application URL by running these commands:

{{- range .Values.app}}
{{$ingressItem:=.ingress}}
{{- range $host := .ingress.hosts }}
  {{- range .paths }}
  http{{ if $ingressItem.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- end}}
