{{- range .Values.app}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "svc-%s" .name }}
  namespace: {{$.Release.Namespace}}
  labels:
    dpName: {{.name}}
    {{- include "ks.labels" $ | nindent 4 }}
spec:
  type: {{ .service.type }}
  ports:
    - port: {{ .service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    dpName: {{.name}}
    {{- include "ks.labels" $ | nindent 4 }}
{{- end }}
