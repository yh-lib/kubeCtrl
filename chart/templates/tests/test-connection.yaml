apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ks.fullname" . }}-test-connection"
  labels:
    {{- include "ks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ks.fullname" . }}']
  restartPolicy: Never
